# =============================================================================
# VARIABLES DE ENTORNO — copiar como .env y ajustar valores
# NUNCA subir .env al repositorio (está en .gitignore)
# =============================================================================

# Base de datos
DB_HOST=localhost
DB_PORT=5432
DB_NAME=academ_sim
DB_USER=postgres
DB_PASSWORD=tu_password_aqui
DB_SCHEMA=academ
DB_MIN_CONNECTIONS=2
DB_MAX_CONNECTIONS=10

# JWT
JWT_SECRET=cambia_esto_por_un_secreto_seguro_de_64_caracteres_minimo
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=480

# Aplicacion
APP_ENV=development
APP_HOST=0.0.0.0
APP_PORT=8000
APP_RELOAD=true
